{
  "name": "n-relay-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "firebase:config": "node tools/build-firebase-config.mjs",
    "serve": "python3 -m http.server 4173",
    "check": "node --check src/app.js && node --check src/firebase-client.js && node --check sw.js && node --check tools/build-firebase-config.mjs"
  }
}
