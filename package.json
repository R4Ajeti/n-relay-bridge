{
  "name": "n-relay-bridge",
  "version": "1.0.20",
  "private": true,
  "type": "module",
  "scripts": {
    "firebase:config": "node tools/build-firebase-config.mjs",
    "version:bump": "node tools/bump-version.mjs",
    "version:write": "node tools/write-app-version.mjs",
    "build:hosting": "node tools/build-hosting.mjs",
    "functions:install": "npm --prefix functions install",
    "functions:check": "node --check functions/index.js",
    "webpush:keys": "npm --prefix functions exec web-push generate-vapid-keys",
    "build": "npm run firebase:config && npm run version:write && npm run build:hosting",
    "deploy": "npm run version:bump && npm run build && firebase deploy --only hosting,database --project n-relay-bridge-db",
    "deploy:all": "npm run version:bump && npm run build && firebase deploy --only hosting,database,functions --project n-relay-bridge-db",
    "deploy:functions": "firebase deploy --only functions --project n-relay-bridge-db",
    "serve": "node tools/dev-server.mjs",
    "check": "node --check src/app.js && node --check src/firebase-client.js && node --check sw.js && node --check tools/bump-version.mjs && node --check tools/build-firebase-config.mjs && node --check tools/build-hosting.mjs && node --check tools/dev-server.mjs && node --check tools/env-utils.mjs && node --check tools/write-app-version.mjs && npm run functions:check"
  }
}
